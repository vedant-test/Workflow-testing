name: testing

on:
  workflow_dispatch:

jobs:
  if_merged:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            console.log("successfully run the workflow")
